/** Global loading shell — shown while page chunks download */
export default function Loading() {
  return (
    <div
      className="min-h-screen bg-gray-bg flex items-center justify-center"
      aria-hidden="true"
    />
  );
}
