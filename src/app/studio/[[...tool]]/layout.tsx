export const metadata = {
  title: "Gray Method Training — Sanity Studio",
  description: "Content management for Gray Method Training",
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
